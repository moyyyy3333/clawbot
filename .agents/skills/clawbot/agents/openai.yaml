interface:
  display_name: "Clawbot"
  short_description: "Repo-specific patterns and workflows for clawbot"
  default_prompt: "Use the clawbot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true